import React from 'react'

const ProfileAccount = ({ className }) => (
  <a href="#" className={`${className || ''} profile-account`}>
    <div className="account-name">
      <span>CONG HAU</span>
    </div>
  </a>
)

export default ProfileAccount
